# Placeholder bot file — real logic will be migrated later.

class MatrixRaidBot:
    def __init__(self, settings, services):
        self.settings = settings
        self.services = services

    async def run(self):
        raise NotImplementedError("Bot logic will be migrated here during refactor.")